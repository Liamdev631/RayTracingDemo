#pragma once

namespace Constants
{
    constexpr int MaxBounceCount = 5;
    constexpr float ShadowBias = 0.01f;
    constexpr float ReflectionIntensity = 0.3f; // Global reflectivity for now
    constexpr float Epsilon = 1e-4f;
}
